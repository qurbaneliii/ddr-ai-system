# Task Interpretation

The primary objective is a credible local MVP, not a claim of production drilling-engineering validation. Exact facts are extracted deterministically with provenance. Language models are optional verbalization/query-understanding providers and cannot overwrite source facts. The supplied corpus is digital PDF-first; OCR is a fallback. Plot results are candidate-level evidence with inspectable overlays.

